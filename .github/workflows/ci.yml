name: Continuous Integration
on: [push]
jobs:
  CI_Steps:
    runs-on: ubuntu-latest
    steps:
      - name: code checkout
        uses: actions/checkout@v4
      - run: echo OK
        name: Code Build
      - run: echo OK
        name: Code Test
      - run: echo OK
        name: Release software

